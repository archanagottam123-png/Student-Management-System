# PostgreSQL connection string (required by the API server and DB scripts)
DATABASE_URL=postgresql://user:password@localhost:5432/student_management

# Port the API server listens on
PORT=8080

# Logging (optional)
NODE_ENV=development
LOG_LEVEL=info
